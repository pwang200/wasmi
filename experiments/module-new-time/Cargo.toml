[package]
name = "module-new-time"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
wasmi = { workspace = true, default-features = false, features = [
    "std",
    "validate",
    "auto-dispatch",
    "stable",
] }
